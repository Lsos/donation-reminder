{
  "name": "@lsos/donation-fund",
  "version": "0.0.1",
  "main": "dist/index.js",
  "lsos": {
    "skipDonationFund": true
  },
  "scripts": {
    "dev": "npm run build:clean && npm run build:watch",
    "build": "npm run build:clean && npm run build:code",
    "build:clean": "rm -rf dist/",
    "build:code": "tsc",
    "build:watch": "tsc --watch",
    "prepublishOnly": "npm run build"
  },
  "dependencies": {
    "@brillout/assert": "^0.1.7"
  },
  "devDependencies": {
    "@babel/core": "^7.0.0",
    "typescript": "^3.9.6"
  },
  "repository": "github:lsos/donation-fund",
  "license": "CC0-1.0",
  "publishConfig": {
    "access": "public"
  }
}
